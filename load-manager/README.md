# Load Manager Package 

## Run 
To run
```bash
go run cmd/load-manager/main.go -q QUEUE_ALGORITHM -s SELECTOR -l STRATEGY -a BACKEND_HOST1:BACKEND_PORT1 -a BACKEND_HOST2:BACKEND_PORT2 ...
```
Remember it's all uppercase for each flag 
